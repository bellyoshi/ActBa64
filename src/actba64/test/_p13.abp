#console
#include "..\CharUtils.abp"
#include "..\StrUtils.abp"
Dim tmp As String
Dim eq As Long
Dim n As Long
Dim key As String
tmp = "#PLATFORM=32"
eq = InStr(1, tmp, "=")
n = eq - 1
ExitProcess(n)
End
