#console
#include "..\CharUtils.abp"
#include "..\StrUtils.abp"
Dim tmp As String
Dim eq As Long
Dim key As String
tmp = "#PLATFORM=32"
eq = InStr(1, tmp, "=")
Print "eq=" + Str$(eq)
Print "tmp=" + tmp
key = Lower$("#PLATFORM")
ExitProcess(0)
End
