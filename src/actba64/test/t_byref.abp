' Expect: 3
' Target: actba64
' ByRef 引数はアドレス渡し（呼び出し後に更新される）
#console
Sub Inc(ByRef x As Long)
    x = x + 1
End Sub

Function AddTo(ByRef x As Long, n As Long) As Long
    x = x + n
    AddTo = x
End Function

Dim a As Long
Dim b As Long
a = 0
Inc(a)
Inc(a)
b = AddTo(a, 1)
ExitProcess(b)
