#console
#include "..\CharUtils.abp"
#include "..\StrUtils.abp"
Dim tmp As String
Dim eq As Long
Dim piece As String
tmp = "#PLATFORM=32"
eq = InStr(1, "xx#PLATFORM=32yy", "#PLATFORM=")
piece = Mid$(tmp, 1, 12)
ExitProcess(Asc(piece))
End
