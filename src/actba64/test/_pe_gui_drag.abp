' Target: actba64
' GUI: UpdateTitle 後の DragAcceptFiles + GetCommandLine（MainWnd_Create 後半）
#USEWINDOW=1

Function WndProc(hWnd As HWND, message As DWord, wParam As WPARAM, lParam As LPARAM) As LRESULT
    Select Case message
        Case WM_CREATE
            SetWindowText(hWnd, "editor - (untitled) [64bit, UTF-8]")
            DragAcceptFiles(hWnd, TRUE)
            Dim cmd As *Byte
            Dim n As Long
            cmd = GetCommandLine()
            If cmd <> 0 Then n = lstrlen(cmd)
            SetWindowText(hWnd, "editor - after drag+cmd")
        Case WM_DESTROY
            PostQuitMessage(0)
        Case Else
            WndProc = DefWindowProc(hWnd, message, wParam, lParam)
            Exit Function
    End Select
    WndProc = 0
End Function

Dim wc As WNDCLASSEX
FillMemory(VarPtr(wc), Len(wc), 0)
wc.cbSize = Len(wc)
wc.hInstance = GetModuleHandle(0)
wc.style = CS_HREDRAW Or CS_VREDRAW
wc.hCursor = LoadCursor(NULL, MAKEINTRESOURCE(IDC_ARROW))
wc.lpszClassName = "NORMAL0"
wc.lpfnWndProc = AddressOf(WndProc)
wc.hbrBackground = CreateSolidBrush(GetSysColor(COLOR_3DFACE))
RegisterClassEx(wc)

CreateWindowEx(&H00000000, "NORMAL0", "editor", &H10cf0000, -2147483648, -2147483648, 495, 367, 0, 0, GetModuleHandle(0), 0)

Dim msg As MSG
Dim r As Long
Do
    r = GetMessage(msg, 0, 0, 0)
    If r = 0 Then Exit Do
    If r = -1 Then Exit Do
    TranslateMessage(msg)
    DispatchMessage(msg)
Loop
End
