#console
#include "..\CharUtils.abp"
#include "..\StrUtils.abp"
Dim tmp As String
Dim eq As Long
Dim n As Long
Dim piece As String
Dim key As String
tmp = "#PLATFORM=32"
eq = InStr(1, tmp, "=")
n = eq - 1
piece = Mid$(tmp, 1, n)
ExitProcess(Asc(Left$(piece,1)))
End
