' Expect: 0
' Target: actba64
' Mid$(s, start, 0) is empty
#console
Dim s As String
Dim n As Long
s = "[" + Mid$("Foo()", 5, 0) + "]"
If s <> "[]" Then
    ExitProcess(1)
End If
n = Len(Mid$("Foo()", 5, 0))
If n <> 0 Then
    ExitProcess(2)
End If
ExitProcess(0)
