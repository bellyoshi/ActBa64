#console
#include "..\CharUtils.abp"
#include "..\StrUtils.abp"
Dim tmp As String
Dim eq As Long
Dim n As Long
Dim value As String
tmp = "#PLATFORM=32"
eq = InStr(1, tmp, "=")
n = Len(tmp) - eq
value = Mid$(tmp, eq + 1, n)
If value <> "32" Then ExitProcess(4)
ExitProcess(20)
End
