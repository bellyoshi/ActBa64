#console
Type T
    a As Long
    b As Long
End Type
Sub ZeroMemory(Destination As VoidPtr, Length As DWord)
    FillMemory(Destination, Length, 0)
End Sub
Function Foo() As Long
    Dim layout[1] As T
    ZeroMemory(VarPtr(layout[0]), SizeOf(T) * 2)
    Foo = 0
End Function
ExitProcess(Foo())
