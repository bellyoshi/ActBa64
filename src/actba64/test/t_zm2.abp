#console
Type T
    a As Long
End Type
Sub ZeroMemory(Destination As VoidPtr, Length As DWord)
    FillMemory(Destination, Length, 0)
End Sub
Function Foo() As Long
    Dim layout As T
    ZeroMemory(VarPtr(layout), SizeOf(T))
    Foo = 0
End Function
ExitProcess(Foo())
