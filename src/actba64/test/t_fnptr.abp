' Expect: 1
' Target: actba64
' *Function 変数へ AddressOf し、間接呼び出し
#console
Function Add(a As Long, b As Long) As Long
    Add = a + b
End Function

Dim p As *Function(a As Long, b As Long) As Long
Dim r As Long
p = AddressOf(Add)
r = p(1, 2)
If r <> 3 Then
    ExitProcess(0)
End If
ExitProcess(1)
