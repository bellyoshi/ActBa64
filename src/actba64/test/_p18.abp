#console
#include "..\CharUtils.abp"
#include "..\StrUtils.abp"
Dim tmp As String
Dim eq As Long
Dim piece As String
Dim key As String
tmp = "#PLATFORM=32"
eq = InStr(1, tmp, "=")
piece = Mid$(tmp, 1, 9)
key = Lower$(piece)
If key <> "#platform" Then ExitProcess(3)
ExitProcess(88)
End
