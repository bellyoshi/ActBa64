#console
#include "..\CharUtils.abp"
#include "..\StrUtils.abp"
Function IdL(x As Long) As Long
    IdL = x
End Function
Dim tmp As String
Dim eq As Long
tmp = "#PLATFORM=32"
eq = IdL(1)
ExitProcess(Len(Lower$(tmp)))
End
