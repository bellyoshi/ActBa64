#console
Sub F4(a As *Byte, b As *Byte, c As *Byte, d As *Byte)
    If a <> NULL Then free(a)
    If b <> NULL Then free(b)
    If c <> NULL Then free(c)
    If d <> NULL Then free(d)
End Sub

Dim p As *Long
Dim q As *Byte
p = NULL
q = NULL
F4(q, q, p, p)
ExitProcess(0)
