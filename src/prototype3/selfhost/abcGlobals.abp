' abcGlobals.abp - abc の Const / Dim（結合時は Lexer/Emit より前に置く）

Const MAX_ARGS = 8
Const MAX_LINE = 512
Const MAX_VARS = 128
Const MAX_CONSTS = 256
Const MAX_TYPES = 32
Const MAX_FIELDS = 128
Const MAX_APIS = 24
Const MAX_IF = 16
Const MAX_LOOP = 16

Dim gArgs(7) As String
Dim gNArgs As Long
Dim gAsmArgs(7) As String
Dim gSbData As StringBuffer
Dim gSbText As StringBuffer
Dim gSbProc As StringBuffer
Dim gSbRt As StringBuffer
Dim gSbOut As StringBuffer
Dim gSbTmp As StringBuffer
Dim gSbSaved As StringBuffer
Dim gStrCount As Long
Dim gUseMsgBox As Long
Dim gEmittedExit As Long

Dim gVarName(127) As String
Dim gVarOff(127) As Long
Dim gVarSize(127) As Long
Dim gVarCount(127) As Long
Dim gVarIsPtr(127) As Long
Dim gVarIsStr(127) As Long
Dim gVarIsGlobal(127) As Long
Dim gVarTypeIdx(127) As Long
Dim gNVars As Long
Dim gFrameSize As Long
Dim gLabelId As Long
Dim gIfElse(15) As String
Dim gIfEnd(15) As String
Dim gIfSp As Long

Dim gConstName(255) As String
Dim gConstVal(255) As Long
Dim gNConsts As Long

Dim gLoopTop(15) As String
Dim gLoopCont(15) As String
Dim gLoopEnd(15) As String
Dim gForVar(15) As String
Dim gForStep(15) As String
Dim gLoopSp As Long

Dim gInProc As Long
Dim gProcName As String
Dim gProcIsFunc As Long
Dim gProcArgBytes As Long
Dim gRetOff As Long
Dim gSavedNVars As Long
Dim gSavedFrame As Long
Dim gSavedVarName(127) As String
Dim gSavedVarOff(127) As Long
Dim gSavedVarSize(127) As Long
Dim gSavedVarCount(127) As Long
Dim gSavedVarIsPtr(127) As Long
Dim gSavedVarIsStr(127) As Long
Dim gSavedVarIsGlobal(127) As Long
Dim gUseStringRt As Long
Dim gUseHostRt As Long

' Phase 6: ユーザー定義 Type とフィールドのメタデータ
Dim gTypeName(31) As String
Dim gTypeSize(31) As Long
Dim gTypeFirstField(31) As Long
Dim gTypeLastField(31) As Long
Dim gNTypes As Long
Dim gFieldType(127) As Long
Dim gFieldName(127) As String
Dim gFieldOff(127) As Long
Dim gFieldSize(127) As Long
Dim gFieldCount(127) As Long
Dim gFieldNext(127) As Long
Dim gNFields As Long
Dim gInType As Long
Dim gCurType As Long

' 既知 stdcall API の使用状況
Dim gApiUsed(23) As Long
Dim gApiExternLines As String
Dim gIncludeStack(15) As String

Dim gExpandResult As String
Dim gCompileOk As Long
Dim gSrcLine As Long
Dim gSrcLineText As String
